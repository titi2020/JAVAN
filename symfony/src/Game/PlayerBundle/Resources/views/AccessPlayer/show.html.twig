{% extends "GamePlayerBundle::layout_crud.html.twig" %}

{% block content %}
    <h1>Access Player</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.date | date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Player</th>
                <td>{{ entity.player.username }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('accessPlayer') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('accessPlayer_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
