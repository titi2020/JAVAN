{% extends "GamePlayerBundle::layout.html.twig" %}



{% block title %}Página de Snowflake{% endblock %}

{% block content %}

    <div class="jumbotron-center">
        <div class="container">
            {% for type, messages in app.session.flashBag.all %}
                {% for message in messages %}
                    <div class="alert alert-{{ type }}">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}

            <img src="/bundles/gameplayer/images/logo.png">
            
            <h1>Bienvenido a Snowflake!</h1>
            <p>¿Estas preparado para una experienciacon inolvidable en videojuegos de estrategia en tiempo real multijugador masivos en línea?</p>
            <p>Podras jugar a {{ num }} juegos con un solo registro.</p>
            <p><a id="boton-jugar-principal" class="btn btn-lg btn-success" role="button" href="{{ path('create') }}">Empieza a juegar YA!</a></p>

        </div>
    </div>
{% endblock %}


{% block content2 %}
    <div class="container">
        <div class="centrar">
            <h2>Los mejores juegos</h2>
            <p>¡No hay huevos a jugar!</p>
        </div>
        <div class="input-group">

            <span class="input-group-btn">
                <button class="btn btn-danger" type="button">Buscar</button>
            </span>
            <input type="text" class="form-control" placeholder="Buscar juego...">
        </div>
        <hr>


        <div class="row">
            {% for game in games %}
                <div class="col-md-4">
                    <div class="thumbnail">

                        <h2>{{ game.name }}</h2>
                        <p>{{ game.description }}</p>
                        <p>
                            <a id="boton-jugar-principal" class="btn btn-primary" href="" role="button">Jugar</a>
                        </p>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}